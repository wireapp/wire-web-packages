name: Test, Build & Release

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  id-token: write
  contents: write

jobs:
  test_build_release:
    uses: ./.github/workflows/reusable_release.yml
    secrets: inherit
    with:
      mode: main
