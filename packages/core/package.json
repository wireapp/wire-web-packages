{
  "dependencies": {
    "@wireapp/api-client": "0.2.6",
    "@wireapp/protocol-messaging": "1.19.0",
    "@wireapp/store-engine": "0.1.0",
    "bazinga64": "5.0.3",
    "protobufjs": "6.8.0",
    "pure-uuid": "1.4.10",
    "wire-webapp-cryptobox": "6.2.0"
  },
  "description": "Wire for Web's communication core.",
  "files": [
    "dist"
  ],
  "license": "GPL-3.0",
  "main": "./dist/commonjs/index.js",
  "name": "@wireapp/core",
  "repository": "https://github.com/wireapp/wire-web-lerna/tree/master/packages/core",
  "scripts": {
    "clear": "rimraf dist && rimraf .tmp",
    "coverage": "istanbul cover --report html ./node_modules/jasmine/bin/jasmine.js",
    "dist": "yarn clear && tsc",
    "preversion": "yarn && yarn dist && yarn test",
    "version": "yarn dist",
    "postversion": "git push && git push --tags",
    "start": "rimraf .tmp && yarn dist && node index.js",
    "test": "yarn dist && yarn test:node && yarn test:browser",
    "test:node": "cross-env JASMINE_CONFIG_PATH=src/test/node/support/jasmine.json jasmine",
    "test:browser": "echo No operation."
  },
  "types": "./dist/commonjs/index.d.ts",
  "version": "0.0.4"
}
