{
  "bin": {
    "wire-standup-bot": "dist/standup-bot/index.js"
  },
  "dependencies": {
    "@wireapp/bot-api": "0.1.0-beta.2",
    "commander": "2.16.0",
    "logdown": "3.2.3"
  },
  "devDependencies": {
    "cross-env": "5.2.0",
    "jasmine": "3.1.0",
    "rimraf": "2.6.2",
    "typescript": "2.9.2"
  },
  "license": "GPL-3.0",
  "name": "@wireapp/bot-samples",
  "private": true,
  "repository": "https://github.com/wireapp/wire-web-packages/tree/master/packages/bot-samples",
  "scripts": {
    "build": "yarn build:node && yarn build:browser",
    "build:browser": "exit 0",
    "build:node": "tsc",
    "clean": "rimraf dist",
    "dist": "yarn clean && yarn build",
    "start:standup-bot": "cross-env NODE_DEBUG=@wireapp/bot-samples* node dist/standup-bot/index.js",
    "test": "yarn test:node && yarn test:browser",
    "test:browser": "exit 0",
    "test:node": "exit 0",
    "test:project": "yarn dist && yarn test"
  },
  "version": "0.0.1"
}
